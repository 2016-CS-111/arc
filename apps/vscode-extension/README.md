# Arc

Arc connects VS Code to the local Arc backend for chat, inline completions, code actions, project
intelligence, and reviewable task workflows.

Set `arc.backendUrl` to the local backend URL when it differs from `http://127.0.0.1:7331`.
