# Infrastructure

Local infrastructure manifests live here when a milestone needs them.

Milestone 1 does not start PostgreSQL, pgvector, Redis, Ollama, or llama.cpp because the first goal
is to establish a small compiling product foundation.
